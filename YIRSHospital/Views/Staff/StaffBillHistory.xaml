<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:yummy="clr-namespace:Xamarin.Forms.PancakeView;assembly=Xamarin.Forms.PancakeView"
             x:Class="YIRSHospital.Views.Staff.StaffBillHistory"
             
             BackgroundColor="#F4F6F8">

    <ContentPage.Resources>
        <ResourceDictionary>
            <Color x:Key="Primary">#004225</Color>
            <Color x:Key="TextPrimary">#1A202C</Color>
            <Color x:Key="TextSecondary">#718096</Color>
            <Color x:Key="Border">#E2E8F0</Color>
        </ResourceDictionary>
    </ContentPage.Resources>
    <StackLayout>
        <Grid RowDefinitions="Auto,*">

            <!-- Summary Dashboard Card -->
            <yummy:PancakeView Grid.Row="0" BackgroundGradientStartColor="{StaticResource Primary}" Margin="0,5,0,0" 
                               BackgroundGradientEndColor="#006B3C" BackgroundGradientAngle="45" CornerRadius="0,0,20,20" Padding="20,20,20,30" HasShadow="True">
                <StackLayout Spacing="16">
                    <Label Text="Billing Summary" FontSize="18" FontAttributes="Bold" TextColor="White"/>

                    <Grid ColumnDefinitions="*,*,*" RowDefinitions="Auto,Auto" ColumnSpacing="12" RowSpacing="12">
                        <StackLayout Grid.Row="0" Grid.Column="0" Spacing="2">
                            <Label Text="Total Bills" FontSize="12" TextColor="#A7F3D0"/>
                            <Label x:Name="TotalBillsLabel" Text="0" FontSize="18" FontAttributes="Bold" TextColor="White"/>
                        </StackLayout>
                        <StackLayout Grid.Row="0" Grid.Column="1" Spacing="2">
                            <Label Text="Pending" FontSize="12" TextColor="#A7F3D0"/>
                            <Label x:Name="TotalPendingLabel" Text="0" FontSize="18" FontAttributes="Bold" TextColor="White"/>
                        </StackLayout>
                        <StackLayout Grid.Row="0" Grid.Column="2" Spacing="2">
                            <Label Text="Paid" FontSize="12" TextColor="#A7F3D0"/>
                            <Label x:Name="TotalPaidLabel" Text="0" FontSize="18" FontAttributes="Bold" TextColor="White"/>
                        </StackLayout>
                    </Grid>

                    <BoxView HeightRequest="1" BackgroundColor="#34D399" Opacity="0.3" Margin="0,5"/>

                    <Grid ColumnDefinitions="*,Auto">
                        <Label Grid.Column="0" Text="Grand Total Raised" TextColor="#A7F3D0" FontSize="14" VerticalOptions="Center"/>
                        <Label Grid.Column="1" x:Name="GrandTotalLabel" Text="₦0.00" TextColor="White" FontSize="20" FontAttributes="Bold" VerticalOptions="Center"/>
                    </Grid>
                </StackLayout>
            </yummy:PancakeView>

            <!-- Bill Groups List -->
            <RefreshView Grid.Row="1" x:Name="HistoryRefreshView" RefreshColor="{StaticResource Primary}" Refreshing="OnRefreshTriggered" Margin="16,0,16,0">
                <CollectionView x:Name="BillsCollectionView" SelectionMode="None">
                    <CollectionView.ItemTemplate>
                        <DataTemplate>
                            <yummy:PancakeView BackgroundColor="White" CornerRadius="14" Padding="16" Margin="0,0,0,12" HasShadow="True">
                                <StackLayout Spacing="10">

                                    <!-- Header -->
                                    <Grid ColumnDefinitions="*,Auto">
                                        <StackLayout Grid.Column="0" Spacing="2">
                                            <Label Text="{Binding PatientName}" FontSize="15" FontAttributes="Bold" TextColor="{StaticResource TextPrimary}"/>
                                            <Label Text="{Binding PatientNo, StringFormat='Patient No: {0}'}" FontSize="12" TextColor="{StaticResource TextSecondary}"/>
                                        </StackLayout>
                                        <yummy:PancakeView Grid.Column="1" BackgroundColor="{Binding StatusBgColor}" CornerRadius="6" Padding="8,4" VerticalOptions="Start">
                                            <Label Text="{Binding Status}" TextColor="{Binding StatusColor}" FontSize="11" FontAttributes="Bold"/>
                                        </yummy:PancakeView>
                                    </Grid>

                                    <BoxView HeightRequest="1" BackgroundColor="{StaticResource Border}"/>

                                    <!-- Sub-details -->
                                    <Grid ColumnDefinitions="Auto,*" RowDefinitions="Auto,Auto" RowSpacing="4" ColumnSpacing="12">
                                        <Label Grid.Row="0" Grid.Column="0" Text="Date Raised:" FontSize="12" TextColor="{StaticResource TextSecondary}"/>
                                        <Label Grid.Row="0" Grid.Column="1" Text="{Binding FormattedDate}" FontSize="12" TextColor="{StaticResource TextPrimary}" FontAttributes="Bold"/>

                                        <Label Grid.Row="1" Grid.Column="0" Text="Ref ID:" FontSize="12" TextColor="{StaticResource TextSecondary}"/>
                                        <Label Grid.Row="1" Grid.Column="1" Text="{Binding DisplayId}" FontSize="12" TextColor="{StaticResource Primary}" FontAttributes="Bold"/>
                                    </Grid>

                                    <!-- Nested Services (Using BindableLayout) -->
                                    <StackLayout BindableLayout.ItemsSource="{Binding Services}" Spacing="4" Margin="0,6,0,0">
                                        <BindableLayout.ItemTemplate>
                                            <DataTemplate>
                                                <Grid ColumnDefinitions="*,Auto" BackgroundColor="#F9FAFB" Padding="8">
                                                    <Label Grid.Column="0" Text="{Binding ServiceName}" FontSize="12" TextColor="{StaticResource TextPrimary}" VerticalOptions="Center"/>
                                                    <Label Grid.Column="1" Text="{Binding Amount, StringFormat='₦{0:N2}'}" FontSize="12" FontAttributes="Bold" TextColor="{StaticResource TextPrimary}" VerticalOptions="Center"/>
                                                </Grid>
                                            </DataTemplate>
                                        </BindableLayout.ItemTemplate>
                                    </StackLayout>

                                    <!-- Total Footer -->
                                    <Grid ColumnDefinitions="*,Auto" Margin="0,6,0,0">
                                        <Label Grid.Column="0" Text="{Binding TotalServices, StringFormat='{0} Services'}" FontSize="12" TextColor="{StaticResource TextSecondary}" VerticalOptions="Center"/>
                                        <Label Grid.Column="1" Text="{Binding GrandTotal, StringFormat='₦{0:N2}'}" FontSize="16" FontAttributes="Bold" TextColor="{StaticResource Primary}" VerticalOptions="Center"/>
                                    </Grid>

                                </StackLayout>
                            </yummy:PancakeView>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                    <CollectionView.EmptyView>
                        <StackLayout HorizontalOptions="Center" VerticalOptions="Center" Margin="0,40,0,0" Spacing="8">
                            <Label Text="No Bills Found" FontSize="16" FontAttributes="Bold" TextColor="{StaticResource TextPrimary}" HorizontalTextAlignment="Center"/>
                            <Label Text="Bills you raise will appear here." FontSize="14" TextColor="{StaticResource TextSecondary}" HorizontalTextAlignment="Center"/>
                        </StackLayout>
                    </CollectionView.EmptyView>
                </CollectionView>
            </RefreshView>
        </Grid>
    </StackLayout>
</ContentPage>